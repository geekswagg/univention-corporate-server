product: ucs
release: "5.2"
version: [1]
scope: ucs_5.2-0-errata5.2-1
src: univention-join
fix: 14.1.1
desc: |
 This update addresses the following issues:
 * The scripts `univention-join` and `univention-run-join-scripts`
   now set the umask to 0022 so that customized more restricted
   settings don't cause problems in joinscripts and listener modules.
bug: [56634]
